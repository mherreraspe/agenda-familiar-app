# Frontend

PWA Vue 3 de Agenda Familiar. La primera pantalla implementa la estructura de “Hoy” descrita en la especificación funcional y sirve como base accesible para los módulos del MVP.

```bash
npm install
npm run dev
```

Comandos de comprobación: `npm run typecheck`, `npm run test` y `npm run build`.
